import { partnerClient } from "@/infrastructure/api/partner-client";

export const salesRepresentativeService = {
  getAllByPartner: (partnerId: number) =>   [ { value: "170", label: "Asesor General" }]
}