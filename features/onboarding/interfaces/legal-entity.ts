import { Option } from "@/interfaces/form";

export interface LegalEntityOptions {
    salesRepresentatives: Option[],
    documentTypes: Option[],
    businessTypes: Option[],
    businessSeniority: Option[],
    cities: Option[],
}